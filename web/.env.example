# URL of the axis-server WebSocket endpoint.
# In dev (Vite proxy not used), the default is ws://localhost:8080/ws.
# In production set this to your Railway URL, e.g.:
#   VITE_WS_URL=wss://axis-server-production.up.railway.app/ws
VITE_WS_URL=ws://localhost:8080/ws
